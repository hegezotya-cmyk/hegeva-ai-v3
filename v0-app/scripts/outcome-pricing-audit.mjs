import fs from "node:fs";const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),ui=read("components/business/outcome-pricing.tsx"),logic=read("lib/outcome-pricing.ts"),hub=read("app/business/page.tsx");for(const[ok,name]of [[ui.includes('useWorkspaceData<Invoice>("invoice_documents")')&&ui.includes('status==="paid"'),"paid workspace baseline"],[logic.includes("Math.max(0,outcome-baseline)")&&logic.includes("Math.min"),"bounded fee calculation"],[ui.includes("never changes Stripe")&&ui.includes("nem változik a Stripe"),"Stripe boundary"],[ui.includes('status:"draft"'),"draft only"],[ui.includes("en:")&&ui.includes("hu:")&&ui.includes("de:")&&ui.includes("fr:")&&ui.includes("es:"),"five locales"],[hub.includes('/business/outcome-pricing'),"hub entry"]])if(!ok)throw new Error(`Outcome Pricing audit failed: ${name}`);console.log("Outcome Pricing V1 audit passed: real paid baseline, bounded uplift fee, internal drafts, five locales and unchanged Stripe/payment state")
