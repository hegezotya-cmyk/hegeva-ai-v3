import fs from "node:fs";const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),ui=read("components/business/one-click-business.tsx"),logic=read("lib/one-click-business.ts"),hub=read("app/business/page.tsx");const checks=[[ui.includes('useWorkspaceData<Customer>("customers")')&&ui.includes('useWorkspaceData<Task>("planner")')&&ui.includes('useWorkspaceData<Doc>("invoice_documents")'),"real workspace records"],[ui.includes('status:"review"')&&ui.includes('status:"approved"')&&ui.includes('status:"created"'),"approval state machine"],[logic.includes("validateLaunchBrief")&&logic.includes("customerEmail"),"validated inputs"],[logic.includes("one-click:")&&ui.includes("some(v=>v.sourceId==="),"duplicate protection"],[ui.includes("No email, quote or message is sent")&&ui.includes("semmi nem kerül elküldésre"),"no automatic send"],[ui.includes("en:")&&ui.includes("hu:")&&ui.includes("de:")&&ui.includes("fr:")&&ui.includes("es:"),"five locales"],[hub.includes('/business/launch'),"business hub entry"]];for(const[ok,name]of checks)if(!ok)throw new Error(`One-Click Business audit failed: ${name}`);console.log("One-Click Business V1 audit passed: validated user facts, review and approval gate, duplicate-safe customer/quote/task creation, five locales, and no automatic send")
