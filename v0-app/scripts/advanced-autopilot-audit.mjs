import fs from "node:fs";const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),logic=read("lib/autopilot-v1.ts"),ui=read("components/business/advanced-autopilot-policy.tsx"),runtime=read("components/command-center/intelligence-autopilot.tsx"),hub=read("app/business/page.tsx");for(const[ok,name]of [[logic.includes("dailyLimit")&&logic.includes("Math.min(10")&&logic.includes("allowedKinds"),"bounded policy"],[logic.includes("ownerApprovalRequired:true"),"mandatory approval"],[runtime.includes("canPrepareAutopilot(signal, actions, policy, today)"),"runtime enforcement"],[ui.includes("no external message")&&ui.includes("külső üzenet"),"external boundary"],[ui.includes("en:")&&ui.includes("hu:")&&ui.includes("de:")&&ui.includes("fr:")&&ui.includes("es:"),"five locales"],[hub.includes('/business/autopilot'),"hub entry"]])if(!ok)throw new Error(`Advanced Autopilot audit failed: ${name}`);console.log("Advanced Autopilot V1 audit passed: workspace policy, allowed action kinds, daily cap, runtime enforcement, mandatory approval, audit trail and five locales")
