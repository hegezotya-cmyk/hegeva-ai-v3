import fs from "node:fs";const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),ui=read("components/business/business-network.tsx"),logic=read("lib/business-network.ts"),hub=read("app/business/page.tsx");for(const[ok,name]of [[ui.includes('useWorkspaceData<Customer>("customers")')&&ui.includes('useWorkspaceData<NetworkRelation>("business_network")'),"real private workspace"],[logic.includes("fromId!==relation.toId")&&logic.includes("relationKey"),"validation and duplicate guard"],[ui.includes("does not discover people")&&ui.includes("nem keres embereket"),"external honesty"],[ui.includes("en:")&&ui.includes("hu:")&&ui.includes("de:")&&ui.includes("fr:")&&ui.includes("es:"),"five locales"],[hub.includes('/business/network'),"hub entry"]])if(!ok)throw new Error(`Business Network audit failed: ${name}`);console.log("Business Network V1 audit passed: real customer relationships, private workspace persistence, duplicate guard, five locales and zero external contact")
