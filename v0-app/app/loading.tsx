export default function Loading() {
  // Keep the current route visible during navigation. Rendering a second AppShell
  // here duplicates navigation and loading copy in streamed crawler HTML.
  return null;
}
